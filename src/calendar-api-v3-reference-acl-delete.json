{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Delete Calendar ACL Rule",
  "description": "Deletes an access control rule for a specified calendar.",
  "type": "object",
  "properties": {
    "endpoint": {
      "type": "string",
      "description": "API endpoint for deleting a calendar ACL rule",
      "value": "https://www.googleapis.com/calendar/v3/calendars/{calendarId}/acl/{ruleId}"
    },
    "method": {
      "type": "string",
      "enum": ["DELETE"],
      "description": "HTTP method used for the request"
    },
    "headers": {
      "type": "object",
      "properties": {
        "Authorization": {
          "type": "string",
          "description": "Bearer token for authentication"
        }
      },
      "required": ["Authorization"]
    },
    "contentType": {
      "type": "string",
      "enum": [],
      "description": "No content type is required for this request"
    },
    "body": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "queryParameters": {
      "type": "object",
      "properties": {},
      "required": []
    },
    "response": {
      "type": "object",
      "properties": {},
      "required": []
    }
  },
  "required": ["endpoint", "method", "headers", "response"],
  "additionalProperties": false
}